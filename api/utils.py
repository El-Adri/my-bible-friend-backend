def detect_intent(_: str) -> str:
    """
    Placeholder MVP. Les endpoints sont explicites (/chat, /advice, /story),
    donc l'intention n'a pas besoin d'etre detectee dynamiquement pour l'instant.
    """
    return "explicit-endpoint"

